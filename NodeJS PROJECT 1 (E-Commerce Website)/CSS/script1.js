const options = {
    bottom : '64px',
    right : '32px',
    left : '32px',
    time : '0.5s',
    mixColor : '#fff',
    backgroundColor : '#fff',
    buttonColorDark : '#100f2c',
    buttonColorLight : '#fff',
    saveInCookies : false,
    label : '🌓'
}

const darkmode = new Darkmode(options);
darkmode.showWidget();

